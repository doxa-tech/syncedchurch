<%= form_for @group, remote: true do |f| %>
    
  <%= render 'error_messages', object: f.object %>

  <%= f.label :group_type %>
  <%= f.select :group_type, options_for_enum(Group, :group_types) %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :place %>
  <%= f.select :place, options_for_enum(Group, :places) %>

  <%= f.submit %>

<% end %>